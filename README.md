# GuardX public OIDC and SARIF fixture

This repository contains synthetic, non-production content used to validate the
GuardX GitHub Actions OIDC, provenance, private SARIF, and GitHub code-scanning
delivery path. The workflow uses GitHub's short-lived workload identity and has
no stored GuardX credential.

All findings and source files here are deliberately harmless test fixtures.
